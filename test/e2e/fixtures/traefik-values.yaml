# traefik-values.yaml
ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`dashboard.localhost`)
    entryPoints:
      - web
providers:
  kubernetesIngress:
    enabled: true
gateway:
  listeners:
    web:
      namespacePolicy:
        from: All