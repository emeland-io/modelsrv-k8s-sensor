---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: systeminstances.structure.emeland.io
spec:
  group: structure.emeland.io
  names:
    kind: SystemInstance
    listKind: SystemInstanceList
    plural: systeminstances
    singular: systeminstance
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SystemInstance is the Schema for the systeminstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SystemInstanceSpec defines the desired state of SystemInstance
            properties:
              displayName:
                description: |-
                  DisplayName is indented as a label to display for humans to recognize this system instance.
                  Machines should use the InstanceId above instead. Ideally this would be unique across an overall IT-System.
                type: string
              instanceId:
                description: InstanceId is the unique identifier of this system instance.
                type: string
              systemId:
                description: SystemId is a reference to the system by UUID, that this
                  instance belongs to.
                type: string
            required:
            - displayName
            - instanceId
            - systemId
            type: object
          status:
            description: SystemInstanceStatus defines the observed state of SystemInstance
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
